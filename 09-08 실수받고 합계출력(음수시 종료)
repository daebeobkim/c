#include <stdio.h>

#define _CRT_SECURE_NO_WARNINGS

int main(void)
{
	int a, b, c, d;
	a = 1;
	int sum = 0;
	for (a = 1;a < 11;a++)
	{
		printf("%d번째 실수를 입력하시오:", a);
		scanf("%d", &b);
		sum += b;
		b++;
		if (b <= 0)
			break;
	}
	printf("합계 = %d", sum);
	return 0;
}
