#include <stdio.h>
#define _CRT_SECURE_NO_WARNINGS

int get_max(int a,int b)
{
	printf("정수를 입력하시오:");
	scanf("%d", &a);
	scanf("%d", &b);
	int max;
	max = a;
	if (b > a)
		max = b;
	return max;
}
int main(void)
{
	int a = 0;
	int b = 0;
	printf("두수 중에서 큰 수는 %d입니다.\n", get_max(a, b));
	return 0;
}
